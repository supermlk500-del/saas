# 织慧通项目说明

## 这是什么

这是一个面向纺织生产场景的系统，项目名称叫 **织慧通**。

它当前主要解决两类问题：

1. **排产**
   - 来料批次进来之后，怎么安排生产
   - 走哪条工艺路线
   - 每道工序安排到哪台设备
   - 计划时间怎么排

2. **质检**
   - 图片离线质检
   - 实时视频流质检
   - 质检结果留档
   - 异常记录和闭环处理

你可以把它理解成一个：

**“订单驱动 + 批次执行 + 质检闭环”的生产管理系统**

---

## 这个项目现在做到了什么

当前已经落地的核心能力有：

- 订单管理
- 订单明细管理
- 订单与批次分配
- 来料批次管理
- 批次资源池管理
- 工艺路线管理
- 工序模板管理
- 设备与设备能力管理
- 订单排产池
- 生产计划管理
- 工序计划管理
- 甘特图排产展示
- 图片离线质检
- 实时视频流质检
- 质检结果查看
- 异常闭环处理

当前系统的主业务链路是：

**订单 -> 订单明细 -> 订单批次分配 -> 来料批次 -> 工艺路线 -> 生产计划 -> 工序计划 -> 质检 -> 异常**

---

## 项目目录怎么理解

项目根目录就是当前仓库目录。文档和业务数据路径优先使用仓库相对路径，避免绑定某一台电脑。

主要目录说明：

- `frontend/`
  - 前端项目
  - 用 Vue 3 + TypeScript + Vite 写的
  - 你平时看到的页面、菜单、表格、按钮都在这里

- `backend/`
  - 后端项目
  - 用 Spring Boot + MyBatis-Plus + MySQL 写的
  - 前端调用的接口、排产逻辑、质检逻辑都在这里

- `docs/`
  - 项目设计文档
  - 包括方案设计、接口设计、数据库设计等
  - 还放着当前使用的 ONNX 模型文件 `best.onnx`

- `photo/`
  - 存放质检图片
  - `upload/`：原始上传图
  - `results/`：后端生成的结果图

---

## 技术栈

### 前端

- Vue 3
- TypeScript
- Vite
- Ant Design Vue
- Axios
- Pinia

### 后端

- Spring Boot 3
- MyBatis-Plus
- MySQL
- WebSocket
- ONNX Runtime

### 模型

当前质检模型位于：

`docs/best.onnx`

后端实际加载路径以 `backend/src/main/resources/application.yml` 中的 `ai.yolo.model-path` 为准。

---

## 运行前你需要准备什么

如果你是完全小白，先准备下面这些环境：

### 1. Node.js

建议版本：

- Node.js 20 或更高

作用：

- 用来运行前端

### 2. Java

建议版本：

- JDK 17

作用：

- 用来运行后端

### 3. Maven

作用：

- 用来构建和启动 Spring Boot 后端

### 4. MySQL

建议版本：

- MySQL 8.x

作用：

- 存放业务数据

---

## 数据库怎么准备

### 1. 创建数据库

数据库名：

`zhihuitong`

### 2. 导入 SQL

导入：

- `docs/zhihuitong.sql`

该文件当前包含 17 张表，已经覆盖 `orderinfo`、`orderitem`、`orderbatchlink`，以及带订单关联字段的 `productionplan`。仓库中不存在独立的 `docs/order_schema_upgrade.sql`，无需执行第二份升级脚本。

---

## 前端怎么启动

### 1. 进入前端目录

```bash
cd frontend
```

### 2. 安装依赖

```bash
npm install
```

### 3. 启动开发环境

```bash
npm run dev
```

### 4. 启动后会发生什么

正常情况下，终端会给你一个本地访问地址，例如：

```bash
http://localhost:5173
```

打开浏览器访问它就能看到前端页面。

---

## 后端怎么启动

### 1. 进入后端目录

```bash
cd backend
```

### 2. 启动后端

```bash
mvn spring-boot:run
```

### 3. 默认端口

后端默认运行在：

```text
http://localhost:8080
```

### 4. 后端当前默认配置

后端关键配置在：

- `backend/src/main/resources/application.yml`
- `backend/src/main/resources/application-dev.yml`

其中已经配置了：

- 数据库连接
- 图片存储路径
- ONNX 模型路径
- WebSocket 视频流检测配置

当前默认图片目录位于仓库的：

- 原图：`photo/upload`
- 结果图：`photo/results`

物理路径以 `application.yml` 的 `inspection.storage` 配置为准，迁移工作区后需要同步调整配置或通过环境配置覆盖。

---

## 前后端怎么连起来

前端调用后端时使用代理前缀：

`/prod-api`

也就是说：

- 前端页面请求的是 `/prod-api/...`
- 实际会被代理到 `http://localhost:8080/...`

所以正常开发时你只需要：

1. 启动 MySQL
2. 启动后端
3. 启动前端

然后打开前端地址即可。

---

## 我应该先看哪些页面

如果你第一次运行这个项目，建议按下面顺序看：

### 1. 来料管理

- 来料批次
- IQC 任务
- IQC 结果

你会看到：

- 批次编号
- 供应商
- 入厂时间
- 重量
- 门幅
- 成分

### 2. 工艺中心

- 工艺路线
- 工序模板
- 设备管理

你会看到：

- 一条路线由哪些工序组成
- 每道工序能用哪些设备

### 3. 排产管理

- 排产池
- 生产计划
- 生产调度甘特图

你会看到：

- 某个批次怎么生成生产计划
- 计划拆成哪些工序
- 每道工序分给哪台设备

### 4. 质量管理

- 实时质检
- 异常闭环
- 返工处理

你会看到：

- 图片离线质检
- 实时视频流质检
- 质检结果记录
- 异常处理链路

---

## 质量检测是怎么做的

当前系统不是用 Python 单独跑质检服务，而是：

**Java 后端内部直接加载 ONNX 模型完成推理**

当前流程大致如下：

### 图片离线质检

1. 前端上传一张图片
2. Java 后端接收图片
3. Java 调用 ONNX Runtime 推理
4. 生成结果图
5. 写入质检记录和附件记录

### 实时视频流质检

1. 前端打开电脑摄像头
2. 前端和后端建立实时检测会话
3. 前端持续发送视频帧
4. 后端持续推理并返回检测框
5. 前端把检测框叠加到视频预览层
6. 关键帧可单独保存为正式质检记录

---

## 你可能最容易困惑的几个点

### 1. 这个系统是订单驱动还是批次驱动？

当前采用的是：

**订单驱动计划 + 批次驱动执行**

订单和订单明细定义生产需求，订单与来料批次完成资源分配后进入排产；生产计划、工序执行、质检和异常则围绕具体批次展开。

### 2. 为什么有些页面看起来比另一些页面更完整？

因为当前版本是按“主业务链路优先”做的。

优先级最高的是：

- 批次
- 工艺
- 排产
- 质检
- 异常

其他外围域（例如订单深度业务）还在后续扩展中。

---

## 常见问题

### 1. 前端能打开，但没有数据

先检查：

- MySQL 是否启动
- 数据库 `zhihuitong` 是否存在
- 是否已经导入 SQL
- 后端是否正常启动

### 2. 前端页面能打开，但按钮没反应

先检查：

- 后端是不是已经启动在 `8080`
- 前端代理是否正常
- 浏览器控制台有没有报错

### 3. 实时质检打不开摄像头

先检查：

- 浏览器是否允许摄像头权限
- 当前页面是否在允许调用摄像头的环境中
- 是否成功选择摄像头资源

### 4. 质检结果图不显示

先检查：

- `photo/results` 目录里是否真的生成了图片
- 后端是否正确返回了相对路径
- 前端是否能访问这些图片路径

### 5. 模型推理报错

先检查：

- `docs/best.onnx` 是否存在
- Java 后端配置里的模型路径是否正确
- ONNX Runtime 依赖是否正常

---

## 重要文档在哪里

如果你要继续理解项目，优先看这些文件：

- [方案设计](C:/Users/lhr/Desktop/saas/docs/方案设计.md)
- [接口详细设计](C:/Users/lhr/Desktop/saas/docs/接口详细设计.md)
- [数据库设计说明](C:/Users/lhr/Desktop/saas/docs/数据库设计说明.md)
- [Java后端开发文档](C:/Users/lhr/Desktop/saas/docs/Java后端开发文档.md)

---

## 一句话总结

如果你完全不懂这个项目，可以先这样理解：

**这是一个给纺织车间使用的系统，用来管理来料批次、安排生产计划、做质量检测，并把异常问题记录和追溯起来。**
